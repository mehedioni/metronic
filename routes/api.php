<?php

/*
|--------------------------------------------------------------------------
| API Routes
|--------------------------------------------------------------------------
|
| Routes registered here are automatically prefixed with "api" and receive
| the "api" middleware group (see bootstrap/app.php). Version the API with
| a route group as endpoints are added, e.g.:
|
|   use Illuminate\Support\Facades\Route;
|
|   Route::prefix('v1')->group(function () {
|       Route::get('/example', ExampleController::class);
|   });
|
| Module-owned API routes live in modules/<Module>/Routes/api.php instead
| (see App\Core\ModuleServiceProvider) and are auto-registered under the
| same "api/v1" prefix.
|
*/
